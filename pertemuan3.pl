%fakta
%aturan

hitung:-
    write('Ini adalah program sederhana'),nl,
    write('Masukan nilai tinggi segitiga = '),read(T),nl,
    write('Masukan nilai alas segitiga = '),read(A),nl,
    Luas is A*T/2,
    write('Nilai tinggi yang anda masukan = '),write(T),nl,
    write('Nilai alas yang anda masukan = '),write(A),nl,
    write('Maka luas segitiga = '),write(Luas).
